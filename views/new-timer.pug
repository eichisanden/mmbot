extends layout

block content
  div.left.row
    a.link(href="/new") New
    span.link  > Timer
  form(method="post", action="/new/timer")
    div.center.row
      input.txt(type="text", name="name")
    div.center.row
      input.txt(type="text", name="command")
    div.center.row
      input.btn(type="submit", value="Save")
